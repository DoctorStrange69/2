Цель тестирования:
Убедиться, что 4 основные математические операции (сложение,вычитание,умножение,деление)
в калькуляторе, разработаном на языке Python работают исправно.


Предпосылки:
Устанолен язык Python и Unittest


Шаги тестирования:
1)В файле CalculatorPy.py на строке 10 после add в скобках указываем  слагаемое 4, после и второе слагаемое 7 через запятую. Сумму 11 указываем после скобок через запятую.

2)В файле CalculatorPy.py на строке 12 после add в скобках указываем  уменьшаемое 10, после и вычитаемое 5 через запятую. Разность 26 указываем после скобок через запятую.

3)В файле CalculatorPy.py на строке 14 после add в скобках указываем произвольный первый множитель 3 и второе множитель 7 через запятую. Произведение 21 указываем после скобок через запятую.

4)В файле CalculatorPy.py на строке 16 после add в скобках указываем делимое 10 и частное 2 через запятую. Частное 5 указываем после скобок через запятую.

5)Вставить команду в терминал: python CalculatorPy.py -v

Ожидаемый результат:
5) В терминале должно быть прописано, что каждая математическая операция с указанными числами соответствует их ответам:
test_add (__main__.CalculatorPy.test_add) ... ok
test_divide (__main__.CalculatorPy.test_divide) ... ok
test_multiply (__main__.CalculatorPy.test_multiply) ... ok
test_subtract (__main__.CalculatorPy.test_subtract) ... ok

----------------------------------------------------------------------
Ran 4 tests in 0.003s

OK

Фактический результат:

Фактический результат соответствует ожидаемому.
